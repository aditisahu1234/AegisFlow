apiVersion: v1
kind: Service

metadata:
  name: aegisflow-gateway

spec:
  selector:
    app.kubernetes.io/name: aegisflow
    app.kubernetes.io/component: gateway

  ports:
    - port: 8080
      targetPort: 8080

  type: ClusterIP